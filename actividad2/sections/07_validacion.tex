\chapter{Validación}

Se revisan el comportamiento del sistema y los archivos generados. La lista de
verificación (tabla siguiente) recorre escena, objetos, scripts, tareas,
movimiento de Bill, acciones visuales, anticolisión, error del estimador y rayos
de sensor. Las capturas 3D se obtuvieron desde CoppeliaSim con
\path{capture_coppeliasim_screenshots.lua}; son las imágenes de las
Figuras~\ref{fig:coppeliasim-captures} y
\ref{fig:phase2-coppeliasim-captures}.

\section{Validación por lista de verificación}

\begingroup
\footnotesize
\setlength{\tabcolsep}{3pt}
\begin{longtable}{P{0.40\textwidth}P{0.45\textwidth}P{0.08\textwidth}}
\toprule
\textbf{Bloque} & \textbf{Verificación} & \textbf{Estado}\\
\midrule
\endhead
Escena & Existe \path{Sim_T2_Phase1_Basic.ttt} y contiene R1, B1, T1, T2, C1. & OK\\
Mannequin & B1 (Bill) se usa como persona móvil tipo \texttt{People/mannequin}. & OK\\
Warehouse & Dos mesas, dos estanterías, sofá, obstáculos y panel de tareas. & OK\\
Scripts & Controlador R1 y manager B1 adjuntos en la escena. & OK\\
Tareas & Entrega y retorno de T1 y T2; cuatro tareas completadas. & OK\\
Bill & Movimiento entre estaciones y acciones de tomar/trabajar/entregar. & OK\\
Percepción & 16 sensores activos y rayos visualizados. & OK\\
Seguridad & Modo \texttt{AVOIDING} observado y señal de riesgo activa. & OK\\
Estimación & Kalman activo, mapa SLAM actualizado y error acotado. & OK\\
Planificación & \texttt{SLAM\_WAYPOINT} observado. & OK\\
Batería & Tarea aceptada por batería y retorno a carga. & OK\\
Capturas CoppeliaSim & Vista general, entrega, trabajo de B1, rayos de sensores y replanificación Fase 2. & OK\\
Fase 2 & Mapa desconocido, robot móvil con rutas aleatorias, cuatro SLAM completados y recuperación del planificador registrada. & OK\\
Deck editable & Existe \path{slides/Actividad2_Pioneer_CoppeliaSim.pptx} junto con su PDF exportado. & OK\\
\bottomrule
\end{longtable}
\endgroup

\section{Timeline de la misión}

La Fig.~\ref{fig:task-timeline} muestra la secuencia temporal exportada con los estados de tarea y movimiento durante la misión.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{phase1_slam_task_timeline.png}
  \caption{Secuencia temporal de la simulación: estados de tarea, movimiento, planificador y acciones de Bill durante la misión.}
  \label{fig:task-timeline}
\end{figure}

La misión incluye esperas de proceso. R1 se detiene mientras Bill trabaja T1 o
T2 y espera a que B1 llegue a WS2 antes de buscar T2. Esa espera aparece como
\texttt{WAIT\_B1}; sin ese estado, la escena presenta movimiento sin
respetar la secuencia lógica de entrega.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{phase1_slam_overview.png}
  \caption{Panel de la ejecución base: pose, error, batería, obstáculos, planificador y estados principales.}
  \label{fig:slam-overview}
\end{figure}

El panel completo de la ejecución base queda recogido en la Fig.~\ref{fig:slam-overview}.

\section{Incidencias corregidas}

Durante la validación se corrigieron tres incidencias.

\subsection{Sincronización de tarea T2}
R1 podía iniciar T2 antes de que Bill llegara a WS2, por lo que la tarea no
representaba una entrega real. La máquina de estados no comprobaba la posición de
Bill al pasar de devolver T1 a recoger T2: la transición ocurría en cuanto R1
terminaba la tarea anterior. Se corrigió bloqueando el estado de solicitud de T2
hasta que Bill llega a WS2.

\subsection{Ciclos del planificador en Fase 2}
Algunas celdas de grid persistentes quedaban demasiado cerca del objetivo y el
planificador entraba en un ciclo de desvíos cortos. Las representaciones de grid
acumulan evidencias pasadas que no se desvanecen, de modo que cuando un obstáculo
desaparece o el objetivo cae dentro del radio de celdas marcadas, la
planificación oscila. El parche exige detecciones persistentes, ignora celdas
pegadas al objetivo y vuelve a ruta directa cuando no hay progreso. Es un límite
conocido de los planificadores sobre grids de ocupación sin olvido temporal.

\subsection{Oscilación del scan matching tipo Hector}
Con pocos impactos de sensor, varios candidatos de pose recibían puntajes
parecidos y la corrección oscilaba. En entornos con poca textura (pasillos
rectos) el scan matching produce un paisaje de puntuación casi plano, donde
mínimas diferencias de ruido invierten el candidato ganador de un ciclo a otro;
es un modo de fallo conocido del ajuste de scan en geometrías poco informativas
\cite{thrun2005}. Se mitigó con una ventana de búsqueda pequeña, un umbral mínimo
de puntuación y una ganancia de corrección parcial.
