\begin{tikzpicture}[
  % --- ESTILOS GLOBALES ---
  call/.style={
    draw=UniPrimaryDeep!40, fill=white, rounded corners=2.5mm, align=left,
    font=\footnotesize\sffamily, text=UniDark, text width=3.2cm, inner sep=6pt,
    % Sombra sutil para separar el texto del fondo
    drop shadow={opacity=0.03, shadow xshift=1pt, shadow yshift=-1pt} 
  },
  arr/.style={
    -{Latex[length=2.5mm,width=1.8mm]}, line width=1.1pt, draw=UniPrimaryDeep!80
  },
  sensor/.style={
    draw=UniPrimaryDeep!65, fill=UniPrimary!10, rounded corners=1.5mm, align=center,
    font=\scriptsize\bfseries\sffamily, text=UniPrimaryDeep, inner sep=3pt
  }
]

  % 1. CHASIS PRINCIPAL (Centrado en 0,0)
  \node[draw=UniPrimaryDeep!60, fill=white, line width=1.2pt, rounded corners=8pt,
        minimum width=4.8cm, minimum height=3.4cm] (chassis) at (0,0) {};

  % 2. MÓDULO PORTAKITS (Interior)
  \node[draw=UniPrimaryDeep!70, fill=UniPrimary!8, line width=0.8pt, rounded corners=4pt,
        minimum width=3.8cm, minimum height=2.4cm, align=center] (payload) at (chassis.center) 
        {\textbf{\small módulo portakits}\\[1.5ex] 
         \scriptsize\color{UniMuted} bandejas normalizadas\\herramientas calibradas};

  % 3. RUEDAS (Calculadas matemáticamente desde las esquinas del chasis)
  \foreach \x/\y in {1/1, 1/-1, -1/1, -1/-1} {
    \node[circle, fill=UniDark!85, draw=UniDark, minimum size=0.55cm, inner sep=0pt] 
      (w\x\y) at ($(chassis.center) + (\x*2.0cm, \y*1.3cm)$) {};
    % Detalle visual interior de la rueda
    \draw[white, line width=0.7pt] ($(w\x\y.center)-(0.15,0.15)$) -- ($(w\x\y.center)+(0.15,0.15)$);
  }

  % 4. SENSORES (Anclados a los bordes exteriores del chasis)
  \node[sensor, fill=GoodGreen!14, draw=GoodGreen!70!black, text=GoodGreen!45!black, 
        minimum width=1.6cm, minimum height=0.4cm, anchor=south] (rfid) at (chassis.north) {RFID/QR};
        
  \node[sensor, fill=WarnAmber!20, draw=WarnAmber!80!black, text=WarnAmber!65!black, 
        minimum width=1.4cm, minimum height=0.4cm, anchor=north] (rgbd) at (chassis.south) {RGB-D};
        
  \node[sensor, minimum width=0.4cm, minimum height=1.4cm, anchor=east] (estop) at (chassis.west) {E\\stop};
  \node[sensor, minimum width=0.4cm, minimum height=1.4cm, anchor=west] (lidar) at (chassis.east) {LiDAR};

  % 5. CAMPO DE SEGURIDAD (Se ajusta automáticamente al tamaño de los componentes)
  \node[draw=UniPrimaryDeep!55, dashed, line width=1.1pt, rounded corners=6pt, inner sep=14pt,
        fit=(chassis) (rfid) (rgbd) (estop) (lidar)] (safety) {};
        
  % Etiqueta del campo de seguridad (cubre la línea punteada)
  \node[font=\scriptsize\bfseries\sffamily, text=UniPrimaryDeep, fill=UniLight, inner sep=2pt] 
        at (safety.north) {campo de seguridad};

  % 6. PANELES DE INFORMACIÓN (Alineados externamente)
  \node[call, anchor=east] (trace) at (-4.2, 2.2) 
    {\textbf{Trazabilidad}\\kit, destino, entrega y devolución.};
    
  \node[call, anchor=east] (seg) at (-4.2, -1.8) 
    {\textbf{Seguridad}\\escáner certificado, bumpers y parada física.};
    
  \node[call, anchor=west] (nav) at (4.2, 2.2) 
    {\textbf{Navegación}\\SLAM con LiDAR, IMU y encoders.};
    
  \node[call, anchor=west] (fod) at (4.2, -1.8) 
    {\textbf{FOD}\\imagen revisable al cerrar ronda.};

  % 7. CONEXIONES (Flujo orgánico con curvas Bezier)
  \draw[arr] (trace.east) to[out=0, in=180] (rfid.west);
  \draw[arr] (seg.east)   to[out=0, in=180] (estop.west);
  \draw[arr] (nav.west)   to[out=180, in=0] (lidar.east);
  \draw[arr] (fod.west)   to[out=180, in=0] (rgbd.east);

  % 8. FONDO GENERAL (Capa inferior)
  \begin{scope}[on background layer]
    \usetikzlibrary{shadows} % Asegura compatibilidad si activas las sombras
    \fill[UniLight, rounded corners=12pt] 
      (trace.south west) ++(-0.6, -1.2) rectangle ($(nav.north east) + (0.6, 1.2)$);
  \end{scope}

\end{tikzpicture}